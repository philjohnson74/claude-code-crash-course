# Claude Code Crash Course

A collection of projects built during the Claude Code crash course.

---

## Projects

### [hookhub/](hookhub/)

A Next.js application built with React 19, TypeScript, and Tailwind CSS.

**Run the dev server:**

```bash
cd hookhub
npm install
npm run dev
```

The app will be available at `http://localhost:3000`.
